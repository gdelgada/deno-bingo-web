# Configuración de la aplicación Bingo Web

# Puerto donde el servidor escuchará (por defecto: 8000)
SERVER_PORT=8000

# Entorno de ejecución (development o production)
DENO_ENV=development

# Directorio público (normalmente no es necesario cambiar esto)
# PUBLIC_DIR=./public
